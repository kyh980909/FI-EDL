# DA-EDL MNIST baseline — follows official DAEDL training protocol.
# Official code: https://github.com/TaeseongYoon/DAEDL
#   lr=1e-3, ExponentialLR(γ=0.95/epoch), 100 epochs, batch=64, ConvNet.

methods: [daedl]
seeds: [0, 1, 2, 3, 4]
run_eval: true
overrides:
  - dataset=mnist
  - backbone=convnet
  - trainer.max_epochs=100
  - data.batch_size=64
  - data.val_from_train=true
  - data.val_split=0.2
  - optimizer.lr=0.001
  - scheduler.name=exponentiallr
  - scheduler.gamma=0.95
  - trainer.precision=32-true
  - trainer.early_stopping=true
  - trainer.early_stopping_monitor=val/loss
  - trainer.early_stopping_mode=min
  - trainer.early_stopping_patience=20
  - eval.confidence_scores=[maxp,maxalpha]
  - logging.wandb.enabled=true
  - logging.wandb.project=FI-EDL-MNIST
  - logging.local_dir=runs/mnist
