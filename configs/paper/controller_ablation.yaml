# Deprecated stub — the original file had no variant overrides so every row
# trained the default FI-EDL config. Use the split presets instead:
#   - controller_constant.yaml     (fisher info, constant gate)
#   - controller_alpha0_gate.yaml  (alpha_0 info, exp gate)
#   - controller_fim_nodetach.yaml (fisher info, exp gate, no stop-grad)
# The FI-EDL default (fisher info + exp gate + detached weight) is covered by
# main_cifar10.yaml, so no fourth variant is needed here.
methods: []
seeds: []
run_eval: false
