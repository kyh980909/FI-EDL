# Paper main results — CIFAR-10 rows (Tables 2, 3, 4).
methods: [edl_l1, edl_l01, edl_l0001, fi_edl, i_edl]
seeds: [0, 1, 2, 3, 4]
run_eval: true
overrides:
  - dataset=cifar10
  - backbone=resnet18
  - trainer.max_epochs=100
  - data.batch_size=64
  - optimizer.lr=0.0005
